/** @deprecated import from src/lib/event-sources — 互換 re-export */
export {
  ensureEventSource,
  extractEnjoytokyoEventId,
  extractGotokyoSpotId,
  type AttachResult,
  type EventSourceName,
  type EventSourceRow,
} from '../../src/lib/event-sources'
